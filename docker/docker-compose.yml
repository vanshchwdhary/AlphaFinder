services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: stock_scout
      POSTGRES_USER: stock_scout
      POSTGRES_PASSWORD: stock_scout
    ports:
      - "5432:5432"
    volumes:
      - stock_scout_pg:/var/lib/postgresql/data

volumes:
  stock_scout_pg:

